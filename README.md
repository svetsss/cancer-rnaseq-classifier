# Классификация типов опухолей по данным RNA-Seq

Учебный проект по анализу высокоразмерных данных экспрессии генов. Текущий этап
охватывает загрузку и первичную проверку датасета UCI Gene Expression Cancer RNA-Seq.
Проект не является медицинской диагностической системой.

## Что реализовано

- загрузка исходного архива UCI без изменения его содержимого;
- разделение матрицы признаков и целевой переменной;
- проверка структуры, числовых типов, пропусков, бесконечных значений и дубликатов;
- подсчёт константных признаков без их удаления;
- сохранение фактической сводки датасета в JSON;
- построение распределения классов;
- офлайн-тесты на малом локальном архиве.

Модели, train/test-разделение, отбор признаков и PCA относятся к следующим этапам и пока
не реализованы.

## Установка

Требуется Python 3.11 или новее.

```bash
python -m venv .venv
source .venv/bin/activate
python -m pip install -e ".[dev]"
```

## Анализ датасета

Команда загружает официальный архив UCI в `data/raw/`, проверяет данные и создаёт два
артефакта:

```bash
python -m scripts.analyze_dataset
```

- `results/dataset_summary.json` — размерность, контроль качества и распределение классов;
- `figures/class_distribution.png` — столбчатая диаграмма числа объектов каждого класса.

Повторный запуск использует локальный архив, только если он открывается как tar.gz и содержит
оба CSV-файла датасета. Повреждённый или неполный файл загружается заново.

## Результат первичного анализа

Фактические характеристики получены командой `python -m scripts.analyze_dataset`:

| Характеристика | Значение |
|---|---:|
| Объекты | 801 |
| Признаки | 20 531 |
| Классы | 5 |
| Пропуски | 0 |
| Бесконечные значения | 0 |
| Повторяющиеся идентификаторы | 0 |
| Повторяющиеся строки признаков | 0 |
| Константные признаки | 267 |

Распределение классов: BRCA — 300, COAD — 78, KIRC — 146, LUAD — 141,
PRAD — 136. Полная сводка, включая SHA-256 исходного архива, находится в
`results/dataset_summary.json`.

## Проверка

```bash
python -m ruff format --check .
python -m ruff check .
python -m mypy src
python -m pytest -q
```

## Источник данных

Fiorini, S. (2016). *Gene Expression Cancer RNA-Seq*. UCI Machine Learning Repository,
dataset 401. DOI: [10.24432/C5R88H](https://doi.org/10.24432/C5R88H).

Датасет распространяется по лицензии CC BY 4.0. Имена признаков условные (`gene_0`,
`gene_1`, ...), поэтому результаты этого проекта нельзя интерпретировать как выявление
конкретных биологических маркеров.
