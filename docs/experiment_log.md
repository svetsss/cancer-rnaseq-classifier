# Журнал экспериментов

## 2026-07-16 — первичный анализ UCI RNA-Seq

Цель запуска — загрузить исходный архив UCI, отделить целевую переменную от матрицы
признаков, проверить структуру данных и сохранить воспроизводимые артефакты этапа 1.

Команда:

```bash
python -m scripts.analyze_dataset
```

Окружение: Python 3.12.13, NumPy 1.26.4, pandas 3.0.3, Matplotlib 3.11.0.

Получены следующие характеристики:

- 801 объект и 20 531 числовой признак типа `float64`;
- 5 классов: BRCA — 300, COAD — 78, KIRC — 146, LUAD — 141, PRAD — 136;
- 0 пропусков и 0 бесконечных значений;
- 0 повторяющихся идентификаторов, имён признаков и строк признаков;
- целевая переменная сохранена отдельно под именем `cancer_type` и отсутствует в `X`.

SHA-256 загруженного архива:
`e6ea3628f9656efa2d7cf382bf5a403e2cc214df3d90312b1928f5650b43a559`.

Созданы `results/dataset_summary.json` и `figures/class_distribution.png`. Исходный
архив сохранён в `data/raw/` и исключён из Git.

Обычный запуск тестов не обращается к сети: загрузчик проверяется на малом архиве,
созданном во временном каталоге. Итог проверки: Ruff — без ошибок, mypy — без ошибок,
pytest — 6 тестов пройдено.

## 2026-07-16 — уточнение первичного анализа

После технического ревью полный анализ выполнен повторно. В 20 531 признаке найдено 267
константных признаков. Они зафиксированы в `results/dataset_summary.json` и не удалялись.
Размерность, распределение классов, SHA-256 архива и остальные проверки качества не
изменились.

Перед использованием локального архива теперь проверяются формат tar.gz и наличие
`data.csv` и `labels.csv`. Повреждённый или неполный файл заменяется повторно загруженным
архивом, а скачанный временный файл проверяется до перемещения в `data/raw/`. Сетевой путь
в unit tests заменён локальным потоком байтов. Итог локальной проверки: Ruff и mypy — без
ошибок, pytest — 16 тестов пройдено.

## 2026-07-16 — разделение данных и baseline-модели

Команда запуска:

```bash
python -m scripts.run_baselines
```

Датасет разделён функцией `train_test_split` с `test_size=0.2`, `random_state=42` и
`stratify=y`. Получено 640 объектов в train и 161 объект в test.

| Класс | Train | Test |
|---|---:|---:|
| BRCA | 240 | 60 |
| COAD | 62 | 16 |
| KIRC | 116 | 30 |
| LUAD | 113 | 28 |
| PRAD | 109 | 27 |

Cross-validation выполнена только на train-части: `StratifiedKFold(n_splits=5,
shuffle=True, random_state=42)`. Test set не передавался в preprocessing, обучение или
оценку и не использовался для выбора конфигураций.

Проверены две конфигурации на всех 20 531 признаках:

- E00: `DummyClassifier(strategy="most_frequent", random_state=42)`;
- E01: `StandardScaler` и `LogisticRegression(max_iter=3000, random_state=42)` внутри
  `Pipeline`.

Фактические результаты 5-fold cross-validation:

| ID | Macro F1, mean ± std | Accuracy, mean ± std | Precision macro, mean ± std | Recall macro, mean ± std | Fit mean, с | Score mean, с | Всего, с |
|---|---:|---:|---:|---:|---:|---:|---:|
| E00 | 0.109091 ± 0.000000 | 0.375000 ± 0.000000 | 0.075000 ± 0.000000 | 0.200000 ± 0.000000 | 0.1563 | 0.0027 | 0.83 |
| E01 | 0.995388 ± 0.006527 | 0.995313 ± 0.006250 | 0.993517 ± 0.009372 | 0.997500 ± 0.003333 | 0.3791 | 0.0709 | 2.29 |

DummyClassifier предсказывает только наиболее частый класс. Для precision остальных
классов используется определённое значение 0, поэтому ожидаемая ситуация не создаёт
предупреждения о неопределённой метрике. Logistic Regression завершилась без предупреждения
о сходимости. Константные признаки на этом этапе не удалялись, отбор признаков и настройка
гиперпараметров не выполнялись.

Созданы `results/split_summary.json` и `results/metrics.csv`. Файлы записываются через
временный файл и `Path.replace`; повторный запуск заменяет строки E00 и E01, не добавляя
дубликаты. Финальные test-метрики не вычислялись.

## 2026-07-16 — SelectKBest внутри cross-validation

Команда запуска:

```bash
python -m scripts.run_feature_selection
```

Сохранённое разделение воспроизведено с `test_size=0.2`, `random_state=42` и `stratify=y`.
SHA-256 последовательностей train- и test-индексов совпали с
`results/split_summary.json`. Cross-validation выполнена только на 640 объектах train:
`StratifiedKFold(n_splits=5, shuffle=True, random_state=42)`.

Каждая конфигурация использовала Pipeline в порядке `SelectKBest(f_classif, k)` →
`StandardScaler` → classifier. Selector обучался заново внутри каждого training fold.
Проверены k=50, 100, 200 и 500.

Параметры классификаторов:

- `LogisticRegression(max_iter=3000, random_state=42)`;
- `LinearSVC(C=1.0, dual="auto", max_iter=5000, random_state=42)`.

| ID | Модель | k | Macro F1, mean ± std | Accuracy, mean ± std | Precision macro, mean ± std | Recall macro, mean ± std | Fit mean, с | Score mean, с | Всего, с |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| E02 | LogisticRegression | 50 | 0.997356 ± 0.003239 | 0.996875 ± 0.003827 | 0.998367 ± 0.002000 | 0.996443 ± 0.004359 | 0.3253 | 0.0541 | 1.94 |
| E03 | LogisticRegression | 100 | 0.996055 ± 0.003222 | 0.995313 ± 0.003827 | 0.997551 ± 0.002000 | 0.994704 ± 0.004327 | 0.3088 | 0.0489 | 1.83 |
| E04 | LogisticRegression | 200 | 0.997397 ± 0.003187 | 0.996875 ± 0.003827 | 0.998367 ± 0.002000 | 0.996522 ± 0.004260 | 0.3096 | 0.0488 | 1.83 |
| E05 | LogisticRegression | 500 | 0.998657 ± 0.002685 | 0.998437 ± 0.003125 | 0.999184 ± 0.001633 | 0.998182 ± 0.003636 | 0.3142 | 0.0485 | 1.85 |
| E06 | LinearSVC | 50 | 0.998699 ± 0.002603 | 0.998437 ± 0.003125 | 0.999184 ± 0.001633 | 0.998261 ± 0.003478 | 0.2898 | 0.0613 | 1.79 |
| E07 | LinearSVC | 100 | 0.998699 ± 0.002603 | 0.998437 ± 0.003125 | 0.999184 ± 0.001633 | 0.998261 ± 0.003478 | 0.3228 | 0.0476 | 1.89 |
| E08 | LinearSVC | 200 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 0.3984 | 0.0470 | 2.27 |
| E09 | LinearSVC | 500 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.1299 | 0.0604 | 5.99 |

Для Logistic Regression лучший CV macro F1 получен при k=500. LinearSVC достиг 1.0 при
k=200 и k=500; при равном качестве k=200 требует меньше признаков и меньше времени. E08
выделена как наиболее компактная из конфигураций с максимальным результатом этапа и как
кандидат для финальной оценки. Разница E08 относительно baseline E01 на всех 20 531
признаках составила +0.004612 macro F1.

`f_classif` сообщил о константных признаках внутри folds и о неопределённых F-значениях при
нулевой внутриклассовой дисперсии. Предупреждения не скрывались; SelectKBest продолжил
работу с вычисленными оценками. Предупреждений о несходимости Logistic Regression или
LinearSVC не было, поэтому `max_iter` не изменялся. В локальном окружении Matplotlib
использовал временный cache каталога конфигурации; итоговый PNG создан корректно.

`results/metrics.csv` атомарно обновлён и содержит E00-E09 без дубликатов. Созданы
`src/feature_selection.py`, `scripts/run_feature_selection.py`,
`tests/test_feature_selection.py` и `figures/feature_selection_comparison.png`.

Test set не использовался для обучения, preprocessing, отбора признаков, cross-validation,
выбора k или расчёта метрик. Список выбранных признаков не сохранялся: он различается между
folds, а условные имена `gene_*` не дают оснований для биологической интерпретации.

## 2026-07-16 — PCA, Random Forest и PyTorch MLP

Команда запуска:

```bash
python -m scripts.run_extended_experiments
```

Окружение: Python 3.12.13, NumPy 2.4.6, pandas 3.0.3, scikit-learn 1.9.0,
PyTorch 2.13.0, CPU.

Сохранённое разделение воспроизведено без изменений: 640 объектов train и 161 объект в
закрытом test set. Контрольные суммы последовательностей индексов совпали с
`results/split_summary.json`. E00-E09 прочитаны из `results/metrics.csv` и сохранены без
пересчёта. Все новые оценки получены только на train-части.

Для E10-E15 использовался Pipeline `StandardScaler` →
`PCA(svd_solver="randomized", random_state=42)` → classifier. Проверены 20, 50 и 100
компонент без whitening. Параметры Logistic Regression и LinearSVC не менялись относительно
предыдущих экспериментов. E16 использовал `SelectKBest(f_classif, k=200)` и
`RandomForestClassifier(n_estimators=300, random_state=42, n_jobs=-1)` без scaler и
настройки гиперпараметров.

| ID | Модель | Представление | Macro F1, mean ± std | Accuracy, mean ± std | Precision macro, mean ± std | Recall macro, mean ± std | Fit mean, с | Score mean, с | Всего, с |
|---|---|---:|---:|---:|---:|---:|---:|---:|---:|
| E10 | LogisticRegression | PCA 20 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 1.000000 ± 0.000000 | 0.3943 | 0.0636 | 2.33 |
| E11 | LogisticRegression | PCA 50 | 0.998657 ± 0.002685 | 0.998437 ± 0.003125 | 0.999184 ± 0.001633 | 0.998182 ± 0.003636 | 0.4281 | 0.0632 | 2.50 |
| E12 | LogisticRegression | PCA 100 | 0.998657 ± 0.002685 | 0.998437 ± 0.003125 | 0.999184 ± 0.001633 | 0.998182 ± 0.003636 | 0.4543 | 0.0649 | 2.64 |
| E13 | LinearSVC | PCA 20 | 0.998728 ± 0.002544 | 0.998437 ± 0.003125 | 0.998333 ± 0.003333 | 0.999167 ± 0.001667 | 0.4011 | 0.0605 | 2.35 |
| E14 | LinearSVC | PCA 50 | 0.988925 ± 0.012215 | 0.992188 ± 0.008558 | 0.986066 ± 0.014629 | 0.993116 ± 0.008010 | 0.4406 | 0.0624 | 2.55 |
| E15 | LinearSVC | PCA 100 | 0.991222 ± 0.008227 | 0.993750 ± 0.005846 | 0.988352 ± 0.010758 | 0.994855 ± 0.005021 | 0.4615 | 0.0641 | 2.67 |
| E16 | RandomForestClassifier | SelectKBest 200 | 0.996055 ± 0.003222 | 0.995313 ± 0.003827 | 0.997551 ± 0.002000 | 0.994704 ± 0.004327 | 0.4829 | 0.0744 | 2.83 |
| E17 | PyTorch MLP | SelectKBest 200 | 0.997356 ± 0.003239 | 0.996875 ± 0.003827 | 0.998367 ± 0.002000 | 0.996443 ± 0.004359 | 0.4394 | 0.0001 | 4.00 |

E17 использовал архитектуру 200 → 128 → 64 → 5 с ReLU и dropout 0.3/0.2, Adam с
`learning_rate=0.001`, batch size 32 и максимумом 100 эпох. В каждом внешнем fold его
training-часть дополнительно делилась на inner-train и inner-validation в пропорции 85/15
со стратификацией. SelectKBest и StandardScaler обучались только на inner-train. Early
stopping с `patience=10` и `min_delta=0.0001` использовал только inner-validation;
outer-validation оценивался один раз после восстановления лучших параметров. Число эпох по
folds: 29, 26, 14, 14 и 21, среднее — 20.8. Веса модели не сохранялись.

E10 и E08 получили одинаковый максимальный CV macro F1 1.0. По правилу меньшей размерности
представления E10 с 20 компонентами выбран как кандидат для финальной оценки и
предварительно лучшая конфигурация по train CV. Это преимущество относится к размерности
после преобразования: каждая компонента PCA использует исходное пространство признаков и
не заменяет интерпретируемый список генов. Разница E10 относительно E08 равна 0.000000
macro F1; E16 уступил E08 на 0.003945, E17 — на 0.002644. Нейронная сеть и Random Forest
не дали основания усложнять предварительный выбор.

Итог предварительного сравнения: лучшая SelectKBest-конфигурация — E08, лучшая
PCA-конфигурация — E10, лучший классический алгоритм по правилам сравнения — E10. E08, E09
и E10 разделяют максимальный результат 1.0, а E10 использует среди них наименьшую
размерность представления. Результаты отдельных дополнительных семейств: E16 Random Forest
— 0.996055, E17 MLP — 0.997356 macro F1.

`f_classif` снова сообщил о константных признаках внутри folds и неопределённых F-значениях
при нулевой внутриклассовой дисперсии. Предупреждения не скрывались. Предупреждений о
несходимости классификаторов не было. В локальном окружении Matplotlib использовал
временный каталог конфигурации; изображения созданы корректно.

`results/metrics.csv` содержит ровно E00-E17 без дубликатов. Созданы
`results/mlp_summary.json`, `figures/pca_comparison.png`,
`figures/model_comparison.png` и `figures/pca_train_projection.png`. Последний график
построен только по train-части, является описательным и не использовался для выбора
кандидата. Test set не оценивался, обучение на полном train и test-метрики не выполнялись.

## 2026-07-16 — фиксация кандидата до test evaluation

Команда запуска:

```bash
python -m scripts.freeze_final_candidate
```

По уже сохранённым результатам train CV зафиксирован E10:

```text
StandardScaler
PCA(n_components=20, svd_solver="randomized", random_state=42, whiten=False)
LogisticRegression(max_iter=3000, random_state=42)
```

E08, E09 и E10 имели одинаковые `cv_f1_macro_mean=1.0` и
`cv_f1_macro_std=0.0`. Tie-break применялся в порядке: максимальный CV macro F1, меньшая
размерность внутреннего представления, меньшее время, более простая модель. E10 использует
20 PCA-компонент внутреннего представления, но PCA обучается на всех 20 531 исходных
признаках. Поэтому фиксация не означает выбор 20 генов.

Источник результатов — commit
`0303133aef4ab853c30e7ee142f84067b36c7c98`. Зафиксированные контрольные суммы разделения:

- train: `5b6a7488ce4e227431d25515675bcb8ad544fcf65620e0abbc09d368419e6d34`;
- test: `833cf2ee6a93f35eb303125d093170889a808a62014a0e5e7ffb469eaf9ce0c8`.

Создан `results/final_candidate.json` со значениями `test_evaluated=false` и
`final_model_trained=false`. На этом шаге датасет не загружался: скрипт прочитал только
`results/metrics.csv` и `results/split_summary.json`. Test-признаки и test-метки не читались,
predictions и test-метрики не создавались, модель не обучалась и не сохранялась.

Следующий разрешённый шаг — однократное обучение зафиксированного E10 на полном train и одна
оценка на test. Любая замена кандидата после этой записи нарушит протокол выбора.

## 2026-07-16 — однократная финальная оценка E10

Кандидат был зафиксирован до открытия test set в commit
`b5ebb02b61a7961fc86380f4072e809746af4f44`. Перед загрузкой датасета проверены точная
конфигурация E10, отсутствие всех шести финальных артефактов, сохранённое разделение и
SHA-256 `results/metrics.csv`:
`3d07cbc37d69a912640d782df3f2dd66daeb788c0570114285614f59828c899b`.
Предварительные проверки завершились без ошибок: Ruff format check и Ruff check — без
изменений и ошибок, mypy — без ошибок, pytest — 130 тестов пройдено, `git diff --check` —
без ошибок.

Обучена только заранее выбранная конфигурация E10:

```text
StandardScaler
PCA(n_components=20, svd_solver="randomized", random_state=42, whiten=False)
LogisticRegression(max_iter=3000, random_state=42)
```

Pipeline получил одним вызовом `fit` все 640 train-объектов. После обучения для 161
test-объекта по одному разу получены классы и вероятности. Разделение воспроизведено с
`random_state=42`; контрольные суммы последовательностей индексов:

- train: `5b6a7488ce4e227431d25515675bcb8ad544fcf65620e0abbc09d368419e6d34`;
- test: `833cf2ee6a93f35eb303125d093170889a808a62014a0e5e7ffb469eaf9ce0c8`.

Итоговые test-метрики:

| Macro F1 | Accuracy | Precision macro | Recall macro | Test − CV macro F1 |
|---:|---:|---:|---:|---:|
| 1.000000 | 1.000000 | 1.000000 | 1.000000 | 0.000000 |

Результаты по классам:

| Класс | Precision | Recall | F1 | Support |
|---|---:|---:|---:|---:|
| BRCA | 1.000000 | 1.000000 | 1.000000 | 60 |
| COAD | 1.000000 | 1.000000 | 1.000000 | 16 |
| KIRC | 1.000000 | 1.000000 | 1.000000 | 30 |
| LUAD | 1.000000 | 1.000000 | 1.000000 | 28 |
| PRAD | 1.000000 | 1.000000 | 1.000000 | 27 |

Confusion matrix, строки соответствуют истинным классам, столбцы — предсказанным:

|  | BRCA | COAD | KIRC | LUAD | PRAD |
|---|---:|---:|---:|---:|---:|
| BRCA | 60 | 0 | 0 | 0 | 0 |
| COAD | 0 | 16 | 0 | 0 | 0 |
| KIRC | 0 | 0 | 30 | 0 | 0 |
| LUAD | 0 | 0 | 0 | 28 | 0 |
| PRAD | 0 | 0 | 0 | 0 | 27 |

Правильно классифицированы 161 объект, ошибок нет. Обучение заняло 0.252369 с,
`predict` — 0.068052 с, `predict_proba` — 0.056737 с. Окружение: Python 3.12.13,
NumPy 2.4.6, pandas 3.0.3, scikit-learn 1.9.0, joblib 1.5.3.

Созданы и проверены следующие артефакты:

- `results/final_evaluation.json` —
  `77c4e09c3b10558413e9581fecc38a1433ccaedb2f2ae8ef721fa38ff08575e4`;
- `results/final_classification_report.json` —
  `03a32afdac545e8c28588d837c6a5a295abff0c2aa166007af90d26edde6a9a7`;
- `results/final_test_predictions.csv` —
  `6a595c0ae6d349c7cadf997dd13ca2e7da56c650d859ec50a7a71423d84bba0f`;
- `results/final_confusion_matrix.csv` —
  `f1496625dc81feb98484d4fc3ec396b263c5808302618002de0c43e7b9d56d98`;
- `figures/final_confusion_matrix.png` —
  `185d8eb505fb52c331e52f854826b2fc8718ed8d4f3fd4aacfcd0267c3156fa7`;
- `models/final_e10_pipeline.joblib` —
  `8082bfa3223209971847064053254d9e5c52a2d758c338317f87ad1a026d230a`.

Модель после сохранения загружена обратно; её шаги, параметры и `classes_` совпали с
зафиксированной конфигурацией, а predictions — с исходным результатом. Оценивался только
E10. E08, E09 и другие конфигурации на test не запускались; после получения test-результата
параметры не менялись, tuning и повторная оценка не выполнялись.

Точный результат относится к одному заранее сформированному разделению небольшого
публичного набора. Он не оценивает переносимость на независимую когорту, влияние batch
effects или изменения лабораторного протокола. Условные имена признаков не позволяют
интерпретировать PCA-компоненты как конкретные биомаркеры. Результат не является оценкой
пригодности модели для медицинской диагностики.
